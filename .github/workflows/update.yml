name: Daily Event Update

on:
  schedule:
    - cron: '0 15 * * *' # 每天东京时间 0 点自动运行
  workflow_dispatch:      # 允许你手动点击按钮立刻更新

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run Update Script
        run: |
          # 这里以后会放爬虫脚本的运行指令
          echo "Checking for new events in Tokyo..."
